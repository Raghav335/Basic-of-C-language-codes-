#include<stdio.h>
int main() {
        int i=1,n,sum=0;
        printf("enter the last value");
        scanf("%d",&n);
        while(i<=n)
        {        sum=sum+i;
                i++;
        }
        printf("the sum of %d natural number is %d",n,sum);

        return 0;
}