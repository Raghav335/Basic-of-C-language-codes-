#include<stdio.h>
        int main()
{
        int x,y,sum;
        printf("enter first number");
        scanf("%d",&x);
        printf("enter second number");
        scanf("%d",&y);
        sum=x+y;
        printf("the sum of a number is %d",sum);
        return 0;
}